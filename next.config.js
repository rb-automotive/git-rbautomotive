/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['rbautomotivemobilemechanic.com.au'], // update if needed
  },
};

module.exports = nextConfig;